version: '2.3'
services:
  main:
    build:
      context: ../services/main
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    depends_on:
      - mongo
  gateway:
    build:
      context: ../gateway
      dockerfile: Dockerfile
    ports:
      - '4000:4000'
    depends_on:
      - mongo
      - main
  web:
    build:
      context: ../web
      dockerfile: Dockerfile
      target: server
    depends_on:
      - gateway
  mongo:
    image: mongo:2.6.12
    volumes:
      - holistic_mongo:/data/db
    ports:
      - '27017:27017'

volumes:
  holistic_mongo:
    external: true

