version: '2'
services:
  main:
    build:
      context: ../services/main
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    depends_on:
      - mongo
  gateway:
    build:
      context: ../gateway
      dockerfile: Dockerfile
    ports:
      - '4000:4000'
    depends_on:
      - mongo
      - main
  mongo:
    image: mongo:2.6.12
    volumes:
      - mongo:/data/db
    ports:
      - '27017:27017'

volumes:
  mongo:
    external: true

