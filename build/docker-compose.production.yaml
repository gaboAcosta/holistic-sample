version: '2'
services:
  main:
    environment:
      - NODE_ENV=production
  gateway:
      - NODE_ENV=production
    ports:
      - '4000:4000'
  web:
    environment:
      - NODE_ENV=production
    ports:
      - '8080:8080'
