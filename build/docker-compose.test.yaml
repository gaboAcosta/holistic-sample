version: '2.3'
services:
  chromedriver:
    image: blueimp/chromedriver
    environment:
      - VNC_ENABLED=true
      - VNC_PASSWORD=holistic
      - EXPOSE_X11=true
    ports:
      - 5900:5900
  nightwatch:
    build:
      context: ../e2e
      dockerfile: Dockerfile
    depends_on:
      - chromedriver
      - web
    volumes:
      - ../e2e/src:/e2e/src
  gateway:
    environment:
      - NODE_ENV=test
  web:
    environment:
      - NODE_ENV=test
  main:
    environment:
      - NODE_ENV=test
  gateway:
    environment:
      - NODE_ENV=test